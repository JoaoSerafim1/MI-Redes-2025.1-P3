#PLACEHOLDER
#Funcao que confirma uma compra
def confirmPurchase (purchaseID):

    if(purchaseID != ""):
        return True
    
    return False

#PLACEHOLDER
#Funcao que cancela uma compra (a estacao de carga foi ocupada durente a compra)
def cancelPurchase (purchaseID):

    if(purchaseID != ""):
        return True
    
    return False